# core 
langchain==0.1.3
langchain_openai==0.0.5
openai==1.10
# openai==1.10 passes `proxies=` to httpx.Client, which httpx removed in 0.28.
# Without this bound every LLM backend fails to construct.
httpx<0.28
pydantic

## formatting
# black

## example dependencies
numpy
wikipedia
duckduckgo-search==6.1.0
# duckduckgo-search 6.1.0 requires pyreqwest_impersonate>=0.4.7 and pip resolves
# to 0.5.5, which ships no macOS-arm64 wheel and needs a Rust/cmake toolchain.
# 0.4.7 has prebuilt wheels.
pyreqwest-impersonate==0.4.7
openmeteo_requests==1.2.0
requests_cache==1.2.0
retry_requests==2.0.0

## UI dependencies
# streamlit
